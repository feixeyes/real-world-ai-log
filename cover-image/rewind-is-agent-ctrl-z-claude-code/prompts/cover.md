You are generating a WeChat Official Account cover image.

Canvas requirements:
- Aspect ratio exactly 2.35:1
- Output size target: 1584×672
- No logos, no trademarks, no real product UI screenshots.
- Clean, modern, readable on mobile.

Theme:
- Article: “Rewind 不是撤销文件：把 Agent 的 Ctrl+Z 从‘代码’升级到‘执行’”
- Key idea: rewind = execution control + checkpoints + recoverability (not just file undo).

Visual direction:
- Minimalist tech illustration, flat + slight depth, high contrast.
- Center: a horizontal timeline / pipeline with 4–5 nodes: “plan → dry-run → apply → checkpoint → rewind”
- Add a curved arrow going back to a checkpoint node to imply rewind.
- Left: abstract input (chat bubble / task card). Right: abstract output (document / checklist).
- Background: dark navy to black gradient with subtle grid.

Typography (in-image text, Chinese):
- Main title: Rewind 不是撤销文件
- Subtitle: 把 Agent 的 Ctrl+Z 从“代码”升级到“执行”

Composition:
- Keep text area clean with enough padding.
- Avoid tiny text. Use bold sans-serif Chinese font style.

Deliverable:
- One single image.